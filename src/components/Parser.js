import CanvasDrawer from './CanvasDrawer.js';

    export default function parse($str){
    
        var cd = new CanvasDrawer();
        //zakomentowana linijka wywołuje błąd
        //cd.drawLineAnimate(cd.ctx, 100); 
        console.log("pareser worked");
    }

